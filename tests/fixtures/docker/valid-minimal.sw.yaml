document:
  dsl: '1.0.1'
  namespace: test
  name: valid-minimal
  version: '1.0.0'

do:
  - printMessage:
      run:
        shell:
          command: echo
          arguments:
            - "Valid workflow"