document:
  dsl: '1.0.2'
  namespace: test
  name: schedule-every-seconds
  version: '1.0.0'
schedule:
  every:
    seconds: 2
do:
  - incrementCounter:
      set:
        counter: ${ ($WORKFLOW.input.counter // 0) + 1 }
        executedAt: ${ now() }
