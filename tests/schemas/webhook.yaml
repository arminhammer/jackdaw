openapi: 3.0.0
info:
  title: Webhook API
  version: 1.0.0
paths:
  /webhook:
    post:
      summary: Webhook endpoint
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
